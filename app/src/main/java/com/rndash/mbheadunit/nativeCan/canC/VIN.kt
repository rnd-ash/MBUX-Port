
@file:Suppress("unused", "FunctionName", "ClassName")
package com.rndash.mbheadunit.nativeCan.canC
import com.rndash.mbheadunit.CanFrame // AUTO GEN
import com.rndash.mbheadunit.nativeCan.CanBusNative // AUTO GEN

/**
 *   Generated by db_converter.py
 *   Object for VIN (ID 0x06FA)
**/

object VIN {

    /** 
     *  Returns the most recent Can Frame representing the state
     *  of VIN
    **/
    fun get_frame() : CanFrame? = CanBusNative.getCFrame(CanCAddrs.VIN)

	/** Gets VIN signal part **/
	fun get_vin_msg() : VIN_MSG = when(CanBusNative.getECUParameterC(CanCAddrs.VIN, 6, 2)) {
		 0 -> VIN_MSG.N_DEF
		 1 -> VIN_MSG.LO
		 2 -> VIN_MSG.MID
		 3 -> VIN_MSG.HI
		 else -> throw Exception("Invalid raw value for VIN_MSG")
	}
	
	/** Sets VIN signal part **/
	fun set_vin_msg(f: CanFrame, p: VIN_MSG) {
		checkFrame(f)
		CanBusNative.setFrameParameter(f, 6, 2, p.raw)
	}
	
	/** Gets VIN data **/
	fun get_vin_data() : Int = CanBusNative.getECUParameterC(CanCAddrs.VIN, 8, 56)
	
	/** Sets VIN data **/
	fun set_vin_data(f: CanFrame, p: Int) {
		checkFrame(f)
		CanBusNative.setFrameParameter(f, 8, 56, p)
	}
	
	/**
     * Auto generated function
     * Throws exception if user tries to set a value in a frame
     * Not designated from the correct ECU
    **/
    private fun checkFrame(f: CanFrame) {
        if (f.canID != CanCAddrs.VIN.addr) {
            throw IllegalArgumentException("CAN ID does not match object!")
        }
    }

	override fun toString() = """
		|Frame VIN (0x06FA):
		|	VIN signal part: ${get_vin_msg()}
		|	VIN data: ${get_vin_data()}
	""".trimMargin("|")
}
