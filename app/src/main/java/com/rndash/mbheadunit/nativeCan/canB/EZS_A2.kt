package com.rndash.mbheadunit.nativeCan.canB


import com.rndash.mbheadunit.nativeCan.CanBAddrs
import com.rndash.mbheadunit.nativeCan.CanBusNative



/**
 *    GENERATED BY parse_kt.py
 *    Decoder class for ECU Frame EZS_A2 (ID: 0x0002)
**/
object EZS_A2 {

	/** (Impulse) (Pulses) pulse ring counter left front wheel (48 per revolution) **/
	fun get_riz_vl() : Int = getParam(0, 8)

	/** (Impulse) (Pulses) pulse ring counter right front wheel (48 per revolution) **/
	fun get_riz_vr() : Int = getParam(8, 8)

	/** (1/min) (1 / min) engine speed **/
	fun get_n_mot() : Int = getParam(16, 16)

	/** (°C) (° C) engine coolant temperature **/
	fun get_t_mot() : Int = getParam(32, 8)

	override fun toString(): String {
		return """
			RIZ_VL: ${get_riz_vl()}
			RIZ_VR: ${get_riz_vr()}
			N_MOT: ${get_n_mot()}
			T_MOT: ${get_t_mot()}
		""".trimIndent()
	}


	private fun getParam(o: Int, l: Int) : Int = CanBusNative.getECUParameterB(CanBAddrs.EZS_A2, o, l)
}
