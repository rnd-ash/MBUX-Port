package com.rndash.mbheadunit.nativeCan.canB


import com.rndash.mbheadunit.nativeCan.CanBAddrs
import com.rndash.mbheadunit.nativeCan.CanBusNative



/**
 *    GENERATED BY parse_kt.py
 *    Decoder class for ECU Frame TELEAID_POS4 (ID: 0x03E8)
**/
object TELEAID_POS4 {

	/** (mas) (Mas) Dead reckoning / map matching latitude (- means south) **/
	fun get_dr_mm_lat() : Int = getParam(0, 32)

	/** (mas) (What) Dead reckoning / map matching longitude (- WEST Means) **/
	fun get_dr_mm_long() : Int = getParam(32, 32)

	override fun toString(): String {
		return """
			DR_MM_LAT: ${get_dr_mm_lat()}
			DR_MM_LONG: ${get_dr_mm_long()}
		""".trimIndent()
	}


	private fun getParam(o: Int, l: Int) : Int = CanBusNative.getECUParameterB(CanBAddrs.TELEAID_POS4, o, l)
}
